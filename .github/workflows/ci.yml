name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    name: Core tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run build
      - run: npm test

  coverage:
    name: Coverage thresholds
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run build
      - run: npm run test:coverage

  lsp:
    name: LSP tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            lsp/package-lock.json

      - run: npm ci
      - run: npm run build
      - run: npm ci
        working-directory: lsp
      - run: npm test
        working-directory: lsp

  site:
    name: Site tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            site/package-lock.json

      - run: npm ci
      - run: npm run build
      - run: npm ci
        working-directory: site
      - run: npm test
        working-directory: site

  all-green:
    name: All checks passed
    if: always()
    needs: [test, coverage, lsp, site]
    runs-on: ubuntu-latest
    steps:
      - name: Verify no failures
        run: |
          if [[ "${{ needs.test.result }}"     == "failure" || \
                "${{ needs.test.result }}"     == "cancelled" || \
                "${{ needs.coverage.result }}" == "failure" || \
                "${{ needs.coverage.result }}" == "cancelled" || \
                "${{ needs.lsp.result }}"      == "failure" || \
                "${{ needs.lsp.result }}"      == "cancelled" || \
                "${{ needs.site.result }}"     == "failure" || \
                "${{ needs.site.result }}"     == "cancelled" ]]; then
            exit 1
          fi
