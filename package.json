{
  "name": "qlang-monorepo",
  "private": true,
  "description": "qlang language core and tooling workspaces",
  "type": "module",
  "workspaces": [
    "core",
    "cli",
    "lsp",
    "site",
    "vscode"
  ],
  "scripts": {
    "build": "npm run build -w @kaluchi/qlang-core",
    "test": "npm test --workspaces --if-present",
    "test:coverage": "npm run test:coverage -w @kaluchi/qlang-core",
    "lint": "eslint .",
    "check:conventions": "node scripts/check-conventions.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0",
  "author": "kaluchi",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kaluchi/qlang.git"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.2.0"
  }
}
