{
  "name": "@kaluchi/qlang-site",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/bundle-qlang.mjs && astro dev",
    "build": "node scripts/bundle-qlang.mjs && astro build",
    "preview": "astro preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@astrojs/mdx": "^8.0.1",
    "@kaluchi/qlang-core": "^0.10.0",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "vitest": "^5.0.0"
  }
}
